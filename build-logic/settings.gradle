dependencyResolutionManagement {
  versionCatalogs {
    libs {
      from(files('../gradle/libs.versions.toml'))
    }
  }
  repositories {
    mavenCentral()
  }
}

rootProject.name = 'build-logic'
